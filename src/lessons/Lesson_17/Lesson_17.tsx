import Counter from "components/Counter/Counter.tsx";

export default function Lesson_17() {
  return (
    <section>
      <Counter count={4} onMinus={() => {}} onPlus={() => {}} />
    </section>
  );
}
